name: export-source-temp-8
on:
  push:
    branches:
      - fix/retail-kouhen-transition-protocol-20260917
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/upload-artifact@v6
        with:
          name: branch-source-v77
          path: |
            .
            !.git
          include-hidden-files: true
